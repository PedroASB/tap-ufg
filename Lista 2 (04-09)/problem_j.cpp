#include <bits/stdc++.h>

using namespace std;

// typedef priority_queue<int, vector<pair<int, int>>, greater<pair<int, int>>> min_heap;

// bool operator()  pair_comparison(pair<int, int> a, pair<int, int> b) { 
//     return abs(a.first - a.second) < abs(b.first - b.second);
// } 

// typedef priority_queue<int, vector<pair<int, int>>, pair_comparison> min_heap;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    // min_heap heap;
    // pair<int, int> x;
    // x = make_pair(1, 5);

    // heap.push(make_pair(1, 5));
    // heap.push(make_pair(2, 7));
    // heap.push(make_pair(0, 4));
    // heap.push(make_pair(0, 3));
    // heap.push(make_pair(2, 6));
    // heap.push(make_pair(-1, 10));

    // while (!heap.empty()) {
    //     cout << heap.top().first << ", " << heap.top().second << endl;
    //     heap.pop();
    // }

    return 0;
}